# ============================================
# Configuração do Flask
# ============================================
# Chave secreta para sessões e tokens (OBRIGATÓRIO)
# Gere uma chave aleatória: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-this

# Porta onde a aplicação irá rodar
PORT=5000

# Modo debug (apenas desenvolvimento)
DEBUG=True

# ============================================
# Banco de Dados Principal
# ============================================
# Para desenvolvimento local com SQLite (não precisa configurar)
USE_SQLITE_LOCALLY=True

# Para produção com PostgreSQL (descomente e configure)
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/nome_do_banco
# USE_SQLITE_LOCALLY=False

# ============================================
# Banco de Dados Externo (OAMD - Integrações/Consultas)
# ============================================
# Banco OAMD (Sistema legado com dados de clientes)
# Host: oamd.pactosolucoes.com.br
# Port: 5432
# User: cs_pacto
# Password: pacto@db
# Database: oamd

# Exemplo de configuração:
# EXTERNAL_DB_URL=postgresql://cs_pacto:pacto@db@oamd.pactosolucoes.com.br:5432/oamd

# Proxy SOCKS5 para conexão com banco externo (opcional)
# Use quando precisar túnel SSH para acessar o banco
# EXTERNAL_DB_PROXY_URL=socks5://localhost:50022

# ============================================
# Autenticação (Opcional para desenvolvimento local)
# ============================================
# Auth0 - Se não configurar, autenticação será desabilitada
# AUTH0_ENABLED=True
# AUTH0_DOMAIN=seu-dominio.auth0.com
# AUTH0_CLIENT_ID=seu-client-id
# AUTH0_CLIENT_SECRET=seu-client-secret
# AUTH0_BASE_URL=http://localhost:5000

# ============================================
# Google OAuth (Opcional - para integração com Google Calendar)
# ============================================
# GOOGLE_CLIENT_ID=seu-google-client-id
# GOOGLE_CLIENT_SECRET=seu-google-client-secret
# GOOGLE_REDIRECT_URI=http://localhost:5000/auth/google/callback

# Escopos do Google OAuth (opcional - padrão é apenas login básico)
# O sistema usa AUTORIZAÇÃO INCREMENTAL:
#   - Login: solicita apenas openid, email, profile
#   - Agenda: solicita calendar quando necessário
#   - Drive: solicita drive quando necessário
# GOOGLE_OAUTH_SCOPES=openid email profile

# ============================================
# Feature Toggles (Otimizações)
# ============================================
# Usar versão otimizada do dashboard (elimina N+1 queries)
# Valores: true | false
# Padrão: false (usa versão original)
USE_OPTIMIZED_DASHBOARD=false

# ============================================
# Cloudflare R2 Storage (Opcional - para upload de arquivos)
# ============================================
# CLOUDFLARE_ENDPOINT_URL=https://seu-account-id.r2.cloudflarestorage.com
# CLOUDFLARE_ACCESS_KEY_ID=seu-access-key-id
# CLOUDFLARE_SECRET_ACCESS_KEY=seu-secret-access-key
# CLOUDFLARE_BUCKET_NAME=nome-do-bucket

# ============================================
# Cache (Opcional)
# ============================================
# Para desenvolvimento, usa cache em memória (SimpleCache)
# Para produção, configure Redis:
# CACHE_TYPE=redis
# CACHE_REDIS_URL=redis://localhost:6379/0

# ============================================
# Sentry (Opcional - para monitoramento de erros)
# ============================================
# SENTRY_DSN=https://seu-dsn@sentry.io/projeto-id
# SENTRY_ENVIRONMENT=development

# ============================================
# Configurações de Segurança
# ============================================
# Limite de requisições por minuto (padrão: 100)
# RATE_LIMIT_GLOBAL=100

# ============================================
# Observações
# ============================================
# 1. Para desenvolvimento local, apenas SECRET_KEY é obrigatório
# 2. Copie este arquivo para .env: cp .env.example .env
# 3. Edite o .env com suas configurações
# 4. O .env não deve ser versionado no Git (.gitignore)
